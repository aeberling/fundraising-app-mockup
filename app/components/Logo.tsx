import { FC } from 'react';

const Logo: FC = () => {
  return (
    <div className="flex items-center">
      <span className="ml-2 text-lg font-bold text-white">Generation Web</span>
    </div>
  );
};

export default Logo; 